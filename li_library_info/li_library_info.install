<?php

/**
 * Implements hook_field_schema().
 */
function li_library_info_field_schema($field) {
  $columns = array(
    'value' => array('type' => 'varchar', 'length' => 255, 'not null' => FALSE),
  );
  $indexes = array(
    'value' => array('value'),
  );
  return array(
    'columns' => $columns,
    'indexes' => $indexes,
  );
}
